pub fn process(input: &str) -> u32 {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::process;

    #[test]
    fn can_process_input() {
        let input = todo!();
        let result = process(input);

        assert_eq!(result, todo!());
    }
}
